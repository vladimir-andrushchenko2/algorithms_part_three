#include <iostream>
#include <string>
#include <algorithm>

bool EvenLetter(char letter) {
    return (letter - 'a' + 1) % 2 == 0;
}

int main() {
    std::string first, second;
    
    
    
    return 0;
}
